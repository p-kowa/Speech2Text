<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/background_shape"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_main"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:title="@string/app_name"
        app:layout_constraintTop_toTopOf="parent"/>

    <com.google.android.material.textfield.TextInputLayout
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.ExposedDropdownMenu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:hint="@string/select_method"
        android:textColorHint="@color/white"
        app:boxStrokeColor="@color/white"
        app:hintTextColor="@color/white">

        <AutoCompleteTextView
            android:hint="@string/select_method"
            android:id="@+id/spMethod"
            style="@style/CustomAutoCompleteTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="none"
            android:textColor="@color/white"
            android:popupBackground="@color/blueDarker"
            android:dropDownHeight="wrap_content" />
    </com.google.android.material.textfield.TextInputLayout>

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:scrollbars="none"
        android:fadeScrollbars="false">

        <RadioGroup
            android:id="@+id/rgLanguage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal" />
    </HorizontalScrollView>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/microSwitch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="24dp"
        android:contentDescription="@string/microphone_toggle"
        app:fabSize="auto"
        app:fabCustomSize="80dp"
        app:maxImageSize="48dp"
        app:elevation="8dp"
        app:hoveredFocusedTranslationZ="16dp"
        app:pressedTranslationZ="12dp"
        app:rippleColor="@color/white"
        app:tint="@color/white"
        app:backgroundTint="@color/blue" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/blueLight"
        android:fillViewport="true">

        <EditText
            android:id="@+id/evResult"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="12dp"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:background="@android:color/transparent"
            android:inputType="textMultiLine|textNoSuggestions"
            android:gravity="top|start"
            android:hint="@string/info_not_recording"
            android:textColorHint="#88FFFFFF"
            android:importantForAutofill="no"
            android:scrollbars="vertical"
            android:overScrollMode="always"
            android:minHeight="200dp" />
    </ScrollView>


    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#22000000" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/blueLight"
        android:minHeight="40dp">

        <TextView
            android:id="@+id/tvInfo"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:paddingStart="12dp"
            android:paddingEnd="60dp"
            android:textStyle="italic"
            android:textColor="#CCFFFFFF"
            android:textSize="13sp" />

        <ImageButton
            android:id="@+id/btnShare"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center_vertical|end"
            android:layout_marginEnd="4dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/share_transcription"
            android:src="@android:drawable/ic_menu_share"
            app:tint="@color/white" />
    </FrameLayout>
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:layout_marginBottom="2dp">

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/chip_group_actions"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:singleLine="true"
            app:chipSpacing="8dp">

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_improve"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/improve_text"
                android:textColor="@color/white"
                android:clickable="true"
                android:focusable="true"
                app:chipIcon="@android:drawable/ic_menu_edit"
                app:chipIconTint="@color/white"
                app:chipBackgroundColor="@color/blue"
                app:chipCornerRadius="16dp"
                app:chipMinHeight="40dp"/>

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_summarize"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/summarize_text"
                android:textColor="@color/white"
                android:clickable="true"
                android:focusable="true"
                app:chipIcon="@android:drawable/ic_menu_sort_by_size"
                app:chipIconTint="@color/white"
                app:chipBackgroundColor="@color/blue"
                app:chipCornerRadius="16dp"
                app:chipMinHeight="40dp"/>

            <!-- Translate chips werden dynamisch erstellt basierend auf verfügbaren Sprachen -->

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_ask_question"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/ask_question"
                android:textColor="@color/white"
                android:clickable="true"
                android:focusable="true"
                app:chipIcon="@android:drawable/ic_menu_help"
                app:chipIconTint="@color/white"
                app:chipBackgroundColor="@color/blue"
                app:chipCornerRadius="16dp"
                app:chipMinHeight="40dp"/>


        </com.google.android.material.chip.ChipGroup>
    </HorizontalScrollView>

</LinearLayout>